project_id = "rhoxismvcalqppbnndew"

# Stripe signs its deliveries with its own scheme and never sends a Supabase
# JWT, so the gateway rejected every one of them. Authorization for this
# function is the Stripe signature, verified over the raw body before anything
# reaches the database. This applies to stripe-webhook only — every other
# function keeps the default verify_jwt = true.
[functions.stripe-webhook]
enabled = true
verify_jwt = false

[functions.create-checkout]
enabled = true
verify_jwt = true
import_map = "./functions/create-checkout/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/create-checkout/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/create-checkout/*.html" ]
